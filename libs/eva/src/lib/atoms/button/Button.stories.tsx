import { Meta } from '@storybook/react';
import { Button } from "./Button";

export default {
  component: Button,
  title: 'Atoms/Button',
} as Meta;

export const Default = () => <Button>Button</Button>
